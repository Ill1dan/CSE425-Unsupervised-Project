# VAE for Hybrid Language–Music Clustering

This project explores unsupervised learning using a Variational Autoencoder (VAE)
to learn latent representations from combined music audio features and song lyrics.

## Structure
- `data/` : audio, lyrics, processed features
- `src/` : model, dataset, clustering, evaluation
- `notebooks/` : exploration & visualization
- `results/` : plots and metrics